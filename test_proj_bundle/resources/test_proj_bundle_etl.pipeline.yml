# The main pipeline for test_proj_bundle

resources:
  pipelines:
    test_proj_bundle_etl:
      name: test_proj_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "../src/test_proj_bundle_etl"

      libraries:
        - glob:
            include: ../src/test_proj_bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
